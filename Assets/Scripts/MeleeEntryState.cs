using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MeleeEntryState : State
{
/*    public override void OnEnter(StateMachine _stateMachine)
    {
        base.OnEnter(_stateMachine);

        State nextState = (State)new GroundEntryState();
        stateMachine.SetNextState(nextState);
        Debug.Log("Firing melee entry state");
    }*/
}

//script to put logic to know if what we are firing is correct.
//currently seems to not be used ANYWHERE
